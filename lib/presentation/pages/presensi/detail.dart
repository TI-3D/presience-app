import 'package:flutter/material.dart';

class DetailPresensiPage extends StatelessWidget {
  const DetailPresensiPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const SafeArea(
        child: Scaffold(
      body: Center(
        child: Text("/presensi/detail"),
      ),
    ));
  }
}
